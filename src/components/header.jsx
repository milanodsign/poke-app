import SideMenu from './sideMenu';
import HeaderTop from './headerTop';

const Header = () => {
  return (
    <header>
      <HeaderTop />
      <SideMenu />
    </header>
  );
};

export default Header;
